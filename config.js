var dir = require('path');

global path = {
    root: __dirname,
    utils: dir.join(__dirname, 'utils'),
    public: dir.join(__dirname, 'public'),
    routes: dir.join(__dirname, 'routes'),
    views: dir.join(__dirname, 'views')
};